import 'package:flutter/material.dart';

class ComposePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("compose page"),
    );
  }
}
